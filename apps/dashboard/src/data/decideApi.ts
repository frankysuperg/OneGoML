import type { DecideRequest, DecideResponse } from '../types'

/**
 * POST /decide — not wired yet; backend is out of scope for this scaffold.
 */
export async function decide(_request: DecideRequest): Promise<DecideResponse> {
  throw new Error('not implemented — mock pendiente')
}
